// Replaced at build time by esbuild `define` (build.js) and by vitest `define`
// (vitest.config.js). False in `--production` builds, so anything behind it is
// dropped as dead code instead of shipping.
declare const INSPECTOR_DEV_PARSER: boolean;
